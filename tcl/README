For getting Vivado project parameters (props, pins, nets) you need to use parse_props_nets.tcl script.
1. Run Vivado 
2. Run Sysnthesis
3. Open Synthesized Design
4. Run in Tcl Console command: set args "YOUR GTs CELL NAME PATTERNS"; source /path/to/parse_props_nets.tcl

For example: "YOUR GTs CELL NAME PATTERNS" as "GTYE4_CHANNEL GTYE4_COMMON etc."
